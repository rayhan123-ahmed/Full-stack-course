// static method
class Notification {
    static send(){
        return `sending tesxt......`
    }
}

console.log(Notification.send());

// so the work of static method is class can acces the function directly without help of object
